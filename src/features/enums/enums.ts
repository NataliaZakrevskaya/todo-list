export enum LoadingStatuses {
  Loading = 'loading',
  Succeeded = 'succeeded',
  Idle = 'idle',
  Failed = 'failed',
}

export enum FilterValues {
  All = 'all',
  Active = 'active',
  Completed = 'completed',
}
